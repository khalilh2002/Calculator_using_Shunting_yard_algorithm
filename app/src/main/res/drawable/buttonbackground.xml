<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <!-- Define the properties of the shape here, like color, corners, etc. -->
            <solid android:color="@color/blueBlack"/> <!-- Example solid color -->
            <corners android:radius="25dp"/>  <!-- Example rounded corners -->
            <stroke android:width="1.5dp" android:color="@color/tanAccent"/>
        </shape>
    </item>
</selector>
